import React from "react";
import ReactDOM from "react-dom/client";
import App from "./views/03css_in_js/App";

const root = ReactDOM.createRoot(document.getElementById('root'))
root.render(
  <React.StrictMode>
    <App/>
  </React.StrictMode>
  )
